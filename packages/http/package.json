{
	"name": "@jbootcaba/http",
	"version": "1.0.0",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"license": "MIT",
	"files": [
		"/dist"
	],
	"scripts": {
		"build:clean": "rimraf dist/*",
		"build:esbuild": "ts-node ./build.ts",
		"build": "npm-run-all build:clean build:esbuild"
	},
	"dependencies": {
		"@jbootcaba/context": "*",
		"@jbootcaba/core": "*",
		"axios": "^0.27.2",
		"typescript": "^4.8.4"
	},
	"devDependencies": {
		"@types/axios": "^0.14.0"
	}
}
